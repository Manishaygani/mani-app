import React, { FC, useState, useEffect } from "react";
import styled from "./Line.module.css";
interface ILine {}

const Line: FC<ILine> = ({}) => {
  return <div className={styled.Link}>.</div>;
};

export default Line;
