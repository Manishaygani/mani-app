import React, { FC, useState, useEffect } from "react";
import styled from "./WorksFooter.module.css";
interface IWorksFooter {}

const WorksFooter: FC<IWorksFooter> = ({}) => {
  return <div className={styled.worksFooter}></div>;
};

export default WorksFooter;
